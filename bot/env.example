# Telegram API Credentials (OBBLIGATORIO per Userbot)
# ⚠️ IMPORTANTE: BotFather Token NON funziona qui!
# Ottieni le tue credenziali da: https://my.telegram.org/apps
# Se non riesci ad accedere, prova browser/dispositivo/rete diversa
# Vedi bot/ALTERNATIVE_SETUP.md per soluzioni alternative
TELEGRAM_API_ID=your_api_id_here
TELEGRAM_API_HASH=your_api_hash_here

# Bot Token (NON usato - solo per riferimento)
# ⚠️ Un token BotFather NON può sostituire API ID/Hash!
# TELEGRAM_BOT_TOKEN=

# Telegram String Session (sarà generata al primo avvio)
# Lascia vuoto per generare una nuova sessione
TELEGRAM_STRING_SESSION=

# Canali/Chat da monitorare (separati da virgola)
# Puoi usare: username del canale (es: @channelname), ID numerico, o nome del canale
# Lascia vuoto per monitorare tutti i messaggi
# Per trovare i canali disponibili, usa: npm run bot:setup
# Esempio con canale privato (richiede accesso):
# TELEGRAM_CHANNELS=@trading_signals
# Oppure multipli:
# TELEGRAM_CHANNELS=@trading_signals,@forex_signals
TELEGRAM_CHANNELS=@example_channel,another_channel

# Canale principale (opzionale - per priorità)
# Se specificato, questo canale avrà priorità nel processing
# TELEGRAM_MAIN_CHANNEL=@trading_signals

# TradingView Settings
TRADINGVIEW_HEADLESS=false
# Imposta a true per nascondere il browser (utile per server)

# ═══════════════════════════════════════════════════════════════════════
# BROKER CONFIGURATION (TradingView Broker Integration)
# ═══════════════════════════════════════════════════════════════════════

# Broker da usare (es: fpmarkets, oanda, icmarkets, ecc.)
TRADINGVIEW_BROKER=fpmarkets

# Tipo account (demo o live)
TRADINGVIEW_ACCOUNT_TYPE=demo

# Esecuzione automatica ordini (true = apri ordini automaticamente, false = solo analisi)
AUTO_EXECUTE_ORDERS=false

# Volume di default per ordini (in lot)
DEFAULT_VOLUME=0.01

# Volume minimo (in lot)
MIN_VOLUME=0.01

# Volume massimo (in lot)
MAX_VOLUME=1.0

# Verifica spread (true = monitora ma ignora per entry/exit, false = ignora completamente)
CHECK_SPREAD=false

# Note: Per usare la funzione broker, devi:
# 1. Essere loggato su TradingView
# 2. Avere un account collegato al broker specificato
# 3. Il broker deve supportare TradingView broker integration

# ═══════════════════════════════════════════════════════════════════════
# RISK MANAGEMENT
# ═══════════════════════════════════════════════════════════════════════

# Balance account (in valuta base, es. USD)
ACCOUNT_BALANCE=10000

# Risk per operazione (in percentuale, es. 0.4 per 0.4%)
RISK_PERCENT=0.4

# Pips sopra entry per Break Even (per coprire spread)
BREAKEVEN_PIPS=2

# Intervallo monitoraggio posizioni (in millisecondi, default: 60000 = 1 minuto)
MONITORING_INTERVAL=60000

# ═══════════════════════════════════════════════════════════════════════
# STATISTICS & MONITORING (Notion Integration)
# ═══════════════════════════════════════════════════════════════════════

# Notion API Key (ottieni da https://www.notion.so/my-integrations)
# NOTION_API_KEY=

# Notion Database ID (ID del database dove salvare le statistiche)
# NOTION_DATABASE_ID=

# Abilita Notion integration (true/false)
NOTION_ENABLED=false

# Note: Per configurare Notion:
# 1. Crea un'integrazione su https://www.notion.so/my-integrations
# 2. Crea un database in Notion con queste proprietà:
#    - Date (title)
#    - Total Trades (number)
#    - Win Rate (number)
#    - Net Profit (number)
#    - Profit Factor (number)
#    - Max Drawdown (number)
# 3. Condividi il database con l'integrazione
# 4. Copia API Key e Database ID qui sopra

